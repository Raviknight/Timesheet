node_modules/
.DS_Store
*.log
dist/

# graphify: local-only graph artifact (rebuilds via post-commit hook) and CLAUDE.md backup
graphify-out/
CLAUDE.md.bak
# graphify MCP server config holds a machine-specific interpreter path; keep local
.mcp.json

# Claude Code local session/tooling directory
.claude/

# tax-research scratch (not committed; tax data lives in src/core/tax.js with inline citations)
.tax-research-*.md
